// nlohmann/json is a header-only library. To install:
// 1. Download json.hpp from https://github.com/nlohmann/json/releases/latest
// 2. Place json.hpp in a suitable directory, e.g., third_party/json.hpp or Unittests/main/json.hpp
// 3. Include it in your code: #include "json.hpp"
//
// For CMake, add the directory to your include path:
// target_include_directories(your_target PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/third_party)
//
// Or use vcpkg: vcpkg install nlohmann-json
// And link with: find_package(nlohmann_json CONFIG REQUIRED)
// target_link_libraries(your_target PRIVATE nlohmann_json::nlohmann_json)
